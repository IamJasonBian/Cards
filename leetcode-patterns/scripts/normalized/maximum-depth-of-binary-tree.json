{
  "slug": "maximum-depth-of-binary-tree",
  "number": 104,
  "title": "Maximum Depth of Binary Tree",
  "difficulty": "Easy",
  "category": "Tree",
  "url": "https://leetcode.com/problems/maximum-depth-of-binary-tree/",
  "statement": "Given the root of a binary tree, return its maximum depth. The maximum depth is the number of nodes along the longest path from the root node down to the farthest leaf node.",
  "constraints": [
    "The number of nodes in the tree is in the range [0, 10^4]",
    "−100 <= Node.val <= 100"
  ],
  "signature": {
    "className": "Solution",
    "methodName": "maxDepth",
    "params": [
      {
        "name": "root",
        "pyType": "Optional[TreeNode]",
        "inputKind": "TreeNode"
      }
    ],
    "returns": "int"
  },
  "starterCode": "class Solution:\n    def maxDepth(self, root: Optional[TreeNode]) -> int:\n        # write your solution here\n        pass\n",
  "equality": "exact",
  "visibleExamples": [
    {
      "input": {
        "root": "[3,9,20,null,null,15,7]"
      },
      "output": 3,
      "explanation": "The tree has height 3, with the longest path being 3 → 20 → 15 (or 3 → 20 → 7)"
    },
    {
      "input": {
        "root": "[2,null,3]"
      },
      "output": 2,
      "explanation": "The tree has height 2, with path 2 → 3"
    },
    {
      "input": {
        "root": "[]"
      },
      "output": 0,
      "explanation": "An empty tree has maximum depth 0"
    }
  ],
  "jsonFriendly": false,
  "jsonFriendlyReason": "The root parameter is a TreeNode, which requires a custom tree adapter for serialization/deserialization.",
  "referenceCode": "from collections import deque\n\nclass Solution:\n    def maxDepth(self, root: Optional[TreeNode]) -> int:\n        if not root:\n            return 0\n        return 1 + max(self.maxDepth(root.left), self.maxDepth(root.right))"
}
